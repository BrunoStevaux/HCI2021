const { MessageEmbed, Interaction } = require('discord.js');

module.exports = {
    assign: (interaction) => {
    
        return 1;
    },

    remove: (interaction) => {

        return 0;
    },

    error: (interaction) => {

        return embed;
    }

}